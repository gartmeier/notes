apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: notes-postgresql
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: vault
  target:
    name: notes-postgresql
  dataFrom:
    - extract:
        key: postgresql
